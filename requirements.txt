# COROS 运动数据分析系统 依赖包

# 核心依赖
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
requests>=2.31.0

# 数据可视化（可选）
seaborn>=0.12.0

# 日期处理
python-dateutil>=2.8.2

# JSON处理
simplejson>=3.19.0

# 命令行参数解析（已包含在标准库）
# argparse (标准库)

# 如需连接真实COROS MCP Server，需要：
# - fastmcp (https://github.com/jlowin/fastmcp)
# - httpx
# - pydantic
